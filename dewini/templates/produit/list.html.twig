{% extends 'base.html.twig' %}

{% block body %}
    <!-- Navbar Start -->
    <nav class="navbar navbar-expand-lg bg-white navbar-light sticky-top p-0 wow fadeIn" data-wow-delay="0.1s">
        <!-- Votre code pour le navbar ici -->
    </nav>
    <!-- Navbar End -->

    <!-- Tableau de produits -->
    <div class="container mt-5">
        <table class="table">
            <thead>
                <tr>
                    <th>NOM</th>
                    <th>PRIX</th>
                    <th>IMAGE</th>
                </tr>
            </thead>
           <tbody>
    {% for p in response %}
        <tr>
            <td>{{ p.nom }}</td>
            <td>{{ p.prix }} DT</td>
            <td><img class="img-thumbnail" src="{{ asset('uploads/' ~ p.ImageName) }}" style="max-width: 150px; max-height: 150px;" alt="{{ p.nom }}"></td>
                            <td>
<a href="{{ path('addpanier', {'id': p.id}) }}" style="color: red">Ajouter au panier</a>

                </td>
        </tr>
    {% endfor %}

            </tbody>
        </table>
    </div>

    <!-- Footer -->
    <footer>
        <!-- Votre code pour le footer ici -->
    </footer>
{% endblock %}
